---
title: Examples
---

## astro components of this project 

import DeclarationTable from '@components/mdx/DeclarationTable.astro';
import StdVer from '@components/mdx/StdVer.astro';
import SeeAlso from '@components/mdx/SeeAlso.astro';

### StdVer

<StdVer since="26">
For path, see path format specification.
</StdVer>

<StdVer until="11">
For path, see path format specification.
</StdVer>

#### For path, see <StdVer since="26" inline>path format specification</StdVer>

### DeclarationTable

<DeclarationTable header="print" content={`
(1)
template< class... Args >
void println( std::format_string<Args...> fmt, Args&&... args );

(1) (since C++23)
template< class... Args >
void println( std::format_string<Args...> fmt, Args&&... args );

(2) (since C++23)
template< class... Args >
void println( std::FILE* stream,
              std::format_string<Args...> fmt, Args&&... args );

(3) (since C++26)
void println();

(S) (since C++23) (constexpr since C++26) 
void println( std::FILE* stream );
`} />

### SeeAlso

<SeeAlso 
  items={[
    {
      name: ["sin", "sinf <note>(C++11)</note>", "sinl <note>(C++11)</note>"],
      description: "computes sine (<em>sin(x)</em>) <note>(function)</note>"
    },
    {
      name: ["tan", "tanf <note>(C++11)</note>", "tanl <note>(C++11)</note>"],
      description: "computes tangent (<em>tan(x)</em>) <note>(function)</note>"
    },
    {
      name: ["acos", "acosf <note>(C++11)</note>", "acosl <note>(C++11)</note>"],
      description: "computes arc cosine (<em>arccos(x)</em>) <note>(function)</note>"
    },
    {
      name: "cos(std::complex)",
      description: "computes cosine of a complex number (<code>cos(z)</code>) <note>(function template)</note>",
    },
    {
      name: "cos(std::valarray)",
      description: "applies the function <code>std::cos</code> to each element of valarray <note>(function template)</note>",
    }
  ]}
/>



### style elements

# yyy <note>xxxnote</note>

yyyy2 <note>xxxnote2</note>

### special tables

##### headless

|---|---|
| xx <b>aa</b> <StdVer inline until="11">For path, see path format specification.</StdVer> | yy |
| aa | bb |
